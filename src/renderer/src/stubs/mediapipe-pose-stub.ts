// Stub for @mediapipe/pose — only needed for BlazePose backend.
// We use MoveNet so this is never called at runtime.
export default {}
export const Pose = undefined
export const POSE_CONNECTIONS = []
export const POSE_LANDMARKS = {}
export const POSE_LANDMARKS_LEFT = {}
export const POSE_LANDMARKS_RIGHT = {}
export const POSE_LANDMARKS_NEUTRAL = {}
export const VERSION = '0.0.0'
